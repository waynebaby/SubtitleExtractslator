name: Release Skill Package (Deprecated)

# Deprecated: This workflow is retained temporarily for migration visibility.
# Use NuGet-first workflows instead:
# - .github/workflows/release-nuget-stable.yml
# - .github/workflows/release-nuget-beta.yml
# Final skill package must remain binary-free; runtime acquisition is external.

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write
  pull-requests: read

concurrency:
  group: release-skill-main
  cancel-in-progress: false

jobs:
  release:
    if: ${{ github.event_name == 'workflow_dispatch' || !contains(github.event.head_commit.message, '[skip-release]') }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          ref: ${{ github.ref_name }}

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 9.0.x

      - name: Restore
        run: dotnet restore SubtitleExtractslator.sln

      - name: Build
        run: dotnet build SubtitleExtractslator.sln -c Release --no-restore

      - name: Test
        run: dotnet test SubtitleExtractslator.sln -c Release --no-build

      - name: Resolve bump type from associated PR labels
        id: bump
        uses: actions/github-script@v7
        with:
          script: |
            const owner = context.repo.owner;
            const repo = context.repo.repo;
            let bump = 'patch';
            let prNumber = '';

            if (context.eventName === 'push') {
              const result = await github.rest.repos.listPullRequestsAssociatedWithCommit({
                owner,
                repo,
                commit_sha: context.sha,
              });

              if (result.data.length > 0) {
                const pr = result.data[0];
                prNumber = String(pr.number);
                const labels = pr.labels.map((l) => String(l.name).toLowerCase());

                const hasAny = (candidates) => candidates.some((name) => labels.includes(name));
                if (hasAny(['major', 'semver:major', 'release:major'])) {
                  bump = 'major';
                } else if (hasAny(['minor', 'semver:minor', 'release:minor'])) {
                  bump = 'minor';
                } else {
                  bump = 'patch';
                }
              }
            }

            core.setOutput('bump', bump);
            core.setOutput('pr_number', prNumber);

      - name: Compute release version
        id: version
        shell: pwsh
        run: |
          git fetch --tags --force

          $skillPath = ".github/skills/subtitle-extractslator/SKILL.md"
          if (-not (Test-Path $skillPath)) {
            throw "Skill file not found: $skillPath"
          }

          $skillRaw = Get-Content $skillPath -Raw
          $skillVersionMatch = [regex]::Match($skillRaw, "(?m)^\s*version:\s*([0-9]+\.[0-9]+\.[0-9]+)\s*$")
          if (-not $skillVersionMatch.Success) {
            throw "Unable to read metadata.version from $skillPath"
          }

          $skillVersion = $skillVersionMatch.Groups[1].Value
          $latestTag = (git tag --list 'v*' --sort=-v:refname | Select-Object -First 1)
          $bump = "${{ steps.bump.outputs.bump }}"

          if ([string]::IsNullOrWhiteSpace($latestTag)) {
            $nextVersion = $skillVersion
          }
          else {
            $base = $latestTag.TrimStart('v')
            $parts = $base.Split('.')
            if ($parts.Count -ne 3) {
              throw "Latest tag '$latestTag' is not a semver tag (vX.Y.Z)."
            }

            $major = [int]$parts[0]
            $minor = [int]$parts[1]
            $patch = [int]$parts[2]

            switch ($bump) {
              "major" {
                $major += 1
                $minor = 0
                $patch = 0
              }
              "minor" {
                $minor += 1
                $patch = 0
              }
              default {
                $patch += 1
              }
            }

            $nextVersion = "$major.$minor.$patch"
          }

          $tag = "v$nextVersion"
          $zipNameWinX64 = "subtitle-extractslator-$tag-win-x64.zip"
          $zipNameWinArm64 = "subtitle-extractslator-$tag-win-arm64.zip"
          $zipNameLinuxX64 = "subtitle-extractslator-$tag-linux-x64.zip"
          $zipNameLinuxMuslX64 = "subtitle-extractslator-$tag-linux-musl-x64.zip"
          $zipNameLinuxArm64 = "subtitle-extractslator-$tag-linux-arm64.zip"
          $zipNameLinuxMuslArm64 = "subtitle-extractslator-$tag-linux-musl-arm64.zip"
          $zipNameLinuxArm = "subtitle-extractslator-$tag-linux-arm.zip"
          $zipNameOsxArm64 = "subtitle-extractslator-$tag-osx-arm64.zip"
          $zipNameOsxX64 = "subtitle-extractslator-$tag-osx-x64.zip"
          $releaseAssetUrlWinX64 = "https://github.com/${{ github.repository }}/releases/download/$tag/$zipNameWinX64"
          $releaseAssetUrlWinArm64 = "https://github.com/${{ github.repository }}/releases/download/$tag/$zipNameWinArm64"
          $releaseAssetUrlLinuxX64 = "https://github.com/${{ github.repository }}/releases/download/$tag/$zipNameLinuxX64"
          $releaseAssetUrlLinuxMuslX64 = "https://github.com/${{ github.repository }}/releases/download/$tag/$zipNameLinuxMuslX64"
          $releaseAssetUrlLinuxArm64 = "https://github.com/${{ github.repository }}/releases/download/$tag/$zipNameLinuxArm64"
          $releaseAssetUrlLinuxMuslArm64 = "https://github.com/${{ github.repository }}/releases/download/$tag/$zipNameLinuxMuslArm64"
          $releaseAssetUrlLinuxArm = "https://github.com/${{ github.repository }}/releases/download/$tag/$zipNameLinuxArm"
          $releaseAssetUrlOsxArm64 = "https://github.com/${{ github.repository }}/releases/download/$tag/$zipNameOsxArm64"
          $releaseAssetUrlOsxX64 = "https://github.com/${{ github.repository }}/releases/download/$tag/$zipNameOsxX64"
          $releasesHome = "https://github.com/${{ github.repository }}/releases"

          "version=$nextVersion" >> $env:GITHUB_OUTPUT
          "tag=$tag" >> $env:GITHUB_OUTPUT
          "zip_name_win_x64=$zipNameWinX64" >> $env:GITHUB_OUTPUT
          "zip_name_win_arm64=$zipNameWinArm64" >> $env:GITHUB_OUTPUT
          "zip_name_linux_x64=$zipNameLinuxX64" >> $env:GITHUB_OUTPUT
          "zip_name_linux_musl_x64=$zipNameLinuxMuslX64" >> $env:GITHUB_OUTPUT
          "zip_name_linux_arm64=$zipNameLinuxArm64" >> $env:GITHUB_OUTPUT
          "zip_name_linux_musl_arm64=$zipNameLinuxMuslArm64" >> $env:GITHUB_OUTPUT
          "zip_name_linux_arm=$zipNameLinuxArm" >> $env:GITHUB_OUTPUT
          "zip_name_osx_arm64=$zipNameOsxArm64" >> $env:GITHUB_OUTPUT
          "zip_name_osx_x64=$zipNameOsxX64" >> $env:GITHUB_OUTPUT
          "release_asset_url_win_x64=$releaseAssetUrlWinX64" >> $env:GITHUB_OUTPUT
          "release_asset_url_win_arm64=$releaseAssetUrlWinArm64" >> $env:GITHUB_OUTPUT
          "release_asset_url_linux_x64=$releaseAssetUrlLinuxX64" >> $env:GITHUB_OUTPUT
          "release_asset_url_linux_musl_x64=$releaseAssetUrlLinuxMuslX64" >> $env:GITHUB_OUTPUT
          "release_asset_url_linux_arm64=$releaseAssetUrlLinuxArm64" >> $env:GITHUB_OUTPUT
          "release_asset_url_linux_musl_arm64=$releaseAssetUrlLinuxMuslArm64" >> $env:GITHUB_OUTPUT
          "release_asset_url_linux_arm=$releaseAssetUrlLinuxArm" >> $env:GITHUB_OUTPUT
          "release_asset_url_osx_arm64=$releaseAssetUrlOsxArm64" >> $env:GITHUB_OUTPUT
          "release_asset_url_osx_x64=$releaseAssetUrlOsxX64" >> $env:GITHUB_OUTPUT
          "releases_home=$releasesHome" >> $env:GITHUB_OUTPUT

      - name: Update SKILL metadata.version and release links
        shell: pwsh
        run: |
          $version = "${{ steps.version.outputs.version }}"
          $tag = "${{ steps.version.outputs.tag }}"
          $zipNameWinX64 = "${{ steps.version.outputs.zip_name_win_x64 }}"
          $zipNameWinArm64 = "${{ steps.version.outputs.zip_name_win_arm64 }}"
          $zipNameLinuxX64 = "${{ steps.version.outputs.zip_name_linux_x64 }}"
          $zipNameLinuxMuslX64 = "${{ steps.version.outputs.zip_name_linux_musl_x64 }}"
          $zipNameLinuxArm64 = "${{ steps.version.outputs.zip_name_linux_arm64 }}"
          $zipNameLinuxMuslArm64 = "${{ steps.version.outputs.zip_name_linux_musl_arm64 }}"
          $zipNameLinuxArm = "${{ steps.version.outputs.zip_name_linux_arm }}"
          $zipNameOsxArm64 = "${{ steps.version.outputs.zip_name_osx_arm64 }}"
          $zipNameOsxX64 = "${{ steps.version.outputs.zip_name_osx_x64 }}"
          $assetUrlWinX64 = "${{ steps.version.outputs.release_asset_url_win_x64 }}"
          $assetUrlWinArm64 = "${{ steps.version.outputs.release_asset_url_win_arm64 }}"
          $assetUrlLinuxX64 = "${{ steps.version.outputs.release_asset_url_linux_x64 }}"
          $assetUrlLinuxMuslX64 = "${{ steps.version.outputs.release_asset_url_linux_musl_x64 }}"
          $assetUrlLinuxArm64 = "${{ steps.version.outputs.release_asset_url_linux_arm64 }}"
          $assetUrlLinuxMuslArm64 = "${{ steps.version.outputs.release_asset_url_linux_musl_arm64 }}"
          $assetUrlLinuxArm = "${{ steps.version.outputs.release_asset_url_linux_arm }}"
          $assetUrlOsxArm64 = "${{ steps.version.outputs.release_asset_url_osx_arm64 }}"
          $assetUrlOsxX64 = "${{ steps.version.outputs.release_asset_url_osx_x64 }}"
          $releasesHome = "${{ steps.version.outputs.releases_home }}"
          $projectHome = "https://github.com/${{ github.repository }}"
          $repoSlug = "${{ github.repository }}"

          $skillPath = ".github/skills/subtitle-extractslator/SKILL.md"
          $skillRaw = Get-Content $skillPath -Raw
          $skillRaw = [regex]::Replace($skillRaw, "(?m)^(\s*version:\s*)([0-9]+\.[0-9]+\.[0-9]+)\s*$", ('${1}' + $version))
          Set-Content -Path $skillPath -Value $skillRaw -Encoding utf8

          $binaryMissingPath = ".github/skills/subtitle-extractslator/references/binary-missing.md"
          $binaryMissingRaw = Get-Content $binaryMissingPath -Raw
          $binaryMissingReleaseBlock = @(
            "<!-- release-links:start -->",
            "- Project URL: [$repoSlug]($projectHome)",
            "- Stable index: [packages.released.md](https://github.com/${{ github.repository }}/blob/main/packages.released.md)",
            "- Beta index: [packages.beta.md](https://github.com/${{ github.repository }}/blob/main/packages.beta.md)",
            "- Runtime fallback .nupkg links are maintained inside the package index pages above.",
            "<!-- release-links:end -->"
          ) -join "`n"
          if ($binaryMissingRaw -notmatch "<!-- release-links:start -->") {
            throw "Missing release-links marker in $binaryMissingPath"
          }
          $binaryMissingRaw = [regex]::Replace($binaryMissingRaw, "(?s)<!-- release-links:start -->.*?<!-- release-links:end -->", $binaryMissingReleaseBlock)
          Set-Content -Path $binaryMissingPath -Value $binaryMissingRaw -Encoding utf8

          $readmeEnPath = "README.md"
          $enBlock = @(
            "<!-- release-links:start -->",
            "- Stable package index: [packages.released.md](https://github.com/${{ github.repository }}/blob/main/packages.released.md)",
            "- Stable package index (zh-CN): [packages.released.zh-CN.md](https://github.com/${{ github.repository }}/blob/main/packages.released.zh-CN.md)",
            "- Beta package index: [packages.beta.md](https://github.com/${{ github.repository }}/blob/main/packages.beta.md)",
            "- Beta package index (zh-CN): [packages.beta.zh-CN.md](https://github.com/${{ github.repository }}/blob/main/packages.beta.zh-CN.md)",
            "- Runtime fallback .nupkg links are maintained in the package index pages above.",
            "<!-- release-links:end -->"
          ) -join "`n"
          $enRaw = Get-Content $readmeEnPath -Raw
          if ($enRaw -notmatch "<!-- release-links:start -->") {
            throw "Missing release-links marker in $readmeEnPath"
          }
          $enRaw = [regex]::Replace($enRaw, "(?s)<!-- release-links:start -->.*?<!-- release-links:end -->", $enBlock)
          Set-Content -Path $readmeEnPath -Value $enRaw -Encoding utf8

          $readmeZhPath = "README.zh-CN.md"
          $zhBlock = @(
            "<!-- release-links:start -->",
            "- 稳定通道包索引：[packages.released.md](https://github.com/${{ github.repository }}/blob/main/packages.released.md)",
            "- 稳定通道中文包索引：[packages.released.zh-CN.md](https://github.com/${{ github.repository }}/blob/main/packages.released.zh-CN.md)",
            "- Beta 通道包索引：[packages.beta.md](https://github.com/${{ github.repository }}/blob/main/packages.beta.md)",
            "- Beta 通道中文包索引：[packages.beta.zh-CN.md](https://github.com/${{ github.repository }}/blob/main/packages.beta.zh-CN.md)",
            "- fallback .nupkg 链接统一由上述包索引页维护。",
            "<!-- release-links:end -->"
          ) -join "`n"
          $zhRaw = Get-Content $readmeZhPath -Raw
          if ($zhRaw -notmatch "<!-- release-links:start -->") {
            throw "Missing release-links marker in $readmeZhPath"
          }
          $zhRaw = [regex]::Replace($zhRaw, "(?s)<!-- release-links:start -->.*?<!-- release-links:end -->", $zhBlock)
          Set-Content -Path $readmeZhPath -Value $zhRaw -Encoding utf8

      - name: Pack source-only skill zips
        shell: pwsh
        run: |
          $tag = "${{ steps.version.outputs.tag }}"
          $zipNameWinX64 = "${{ steps.version.outputs.zip_name_win_x64 }}"
          $zipNameWinArm64 = "${{ steps.version.outputs.zip_name_win_arm64 }}"
          $zipNameLinuxX64 = "${{ steps.version.outputs.zip_name_linux_x64 }}"
          $zipNameLinuxMuslX64 = "${{ steps.version.outputs.zip_name_linux_musl_x64 }}"
          $zipNameLinuxArm64 = "${{ steps.version.outputs.zip_name_linux_arm64 }}"
          $zipNameLinuxMuslArm64 = "${{ steps.version.outputs.zip_name_linux_musl_arm64 }}"
          $zipNameLinuxArm = "${{ steps.version.outputs.zip_name_linux_arm }}"
          $zipNameOsxArm64 = "${{ steps.version.outputs.zip_name_osx_arm64 }}"
          $zipNameOsxX64 = "${{ steps.version.outputs.zip_name_osx_x64 }}"
          $packageRoot = Join-Path $env:RUNNER_TEMP "skill-package"

          $targets = @(
            @{ Rid = "win-x64"; Zip = $zipNameWinX64 },
            @{ Rid = "win-arm64"; Zip = $zipNameWinArm64 },
            @{ Rid = "linux-x64"; Zip = $zipNameLinuxX64 },
            @{ Rid = "linux-musl-x64"; Zip = $zipNameLinuxMuslX64 },
            @{ Rid = "linux-arm64"; Zip = $zipNameLinuxArm64 },
            @{ Rid = "linux-musl-arm64"; Zip = $zipNameLinuxMuslArm64 },
            @{ Rid = "linux-arm"; Zip = $zipNameLinuxArm },
            @{ Rid = "osx-arm64"; Zip = $zipNameOsxArm64 },
            @{ Rid = "osx-x64"; Zip = $zipNameOsxX64 }
          )

          foreach ($target in $targets) {
            $rid = $target.Rid
            $zipName = $target.Zip
            $stagingSkillRoot = Join-Path $packageRoot "subtitle-extractslator-$rid"
            $skillRootInZip = Join-Path $stagingSkillRoot "subtitle-extractslator"
            $referencesDir = Join-Path $skillRootInZip "references"
            $soWorkflowDir = Join-Path (Join-Path $skillRootInZip "assets") "so-workflow"

            if (Test-Path $stagingSkillRoot) {
              Remove-Item $stagingSkillRoot -Recurse -Force
            }

            New-Item -ItemType Directory -Path $referencesDir -Force | Out-Null
            New-Item -ItemType Directory -Path $soWorkflowDir -Force | Out-Null

            Copy-Item ".github/skills/subtitle-extractslator/SKILL.md" (Join-Path $skillRootInZip "SKILL.md") -Force
            Copy-Item ".github/skills/subtitle-extractslator/references/*" $referencesDir -Recurse -Force
            Copy-Item ".github/skills/subtitle-extractslator/assets/so-workflow/*" $soWorkflowDir -Recurse -Force

            $zipPath = Join-Path $env:RUNNER_TEMP $zipName
            if (Test-Path $zipPath) {
              Remove-Item $zipPath -Force
            }

            Compress-Archive -Path $skillRootInZip -DestinationPath $zipPath -Force
          }

      - name: Commit version and README updates
        shell: pwsh
        run: |
          $tag = "${{ steps.version.outputs.tag }}"
          $branch = "${{ github.ref_name }}"

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add .github/skills/subtitle-extractslator/SKILL.md .github/skills/subtitle-extractslator/references/binary-missing.md README.md README.zh-CN.md

          if (-not (git diff --cached --quiet)) {
            git commit -m "chore(release): update metadata and release links for $tag [skip-release]"
            git push origin "HEAD:$branch"
          }

      - name: Ensure release tag exists
        shell: pwsh
        run: |
          $tag = "${{ steps.version.outputs.tag }}"

          git fetch --tags --force

          $existsRemote = $false
          $remote = git ls-remote --tags origin "refs/tags/$tag"
          if (-not [string]::IsNullOrWhiteSpace($remote)) {
            $existsRemote = $true
          }

          if ($existsRemote) {
            Write-Host "Tag $tag already exists on origin. Skipping tag creation."
            exit 0
          }

          $existsLocally = $false
          try {
            git rev-parse --verify "refs/tags/$tag" *> $null
            if ($LASTEXITCODE -eq 0) {
              $existsLocally = $true
            }
          }
          catch {
            $existsLocally = $false
          }

          if (-not $existsLocally) {
            git tag $tag
            git push origin $tag
          }

      - name: Publish release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: SubtitleExtractslator ${{ steps.version.outputs.tag }}
          generate_release_notes: true
          prerelease: false
          files: |
            ${{ runner.temp }}/${{ steps.version.outputs.zip_name_win_x64 }}
            ${{ runner.temp }}/${{ steps.version.outputs.zip_name_win_arm64 }}
            ${{ runner.temp }}/${{ steps.version.outputs.zip_name_linux_x64 }}
            ${{ runner.temp }}/${{ steps.version.outputs.zip_name_linux_musl_x64 }}
            ${{ runner.temp }}/${{ steps.version.outputs.zip_name_linux_arm64 }}
            ${{ runner.temp }}/${{ steps.version.outputs.zip_name_linux_musl_arm64 }}
            ${{ runner.temp }}/${{ steps.version.outputs.zip_name_linux_arm }}
            ${{ runner.temp }}/${{ steps.version.outputs.zip_name_osx_arm64 }}
            ${{ runner.temp }}/${{ steps.version.outputs.zip_name_osx_x64 }}
